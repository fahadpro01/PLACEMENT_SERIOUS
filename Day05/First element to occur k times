int firstElementKTime(int a[], int n, int k)
    {   int index=0,i;
    unordered_map<int,int>map;
        for( i=0;i<n;i++){
            map[a[i]]++;
            if(map[a[i]]==k)
                return a[i];
             
            }
             
        
        return -1;
